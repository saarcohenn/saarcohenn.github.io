name: MasterDeployCI
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x]
    steps:
      - uses: action/checkout@v1
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install Packges
        run: npm install
      - name: Deploy to GitHub Packges
        run: |
          git config --global user.email ${{ secrets.MY_EMAIL }}
          git config --global user.name ${{ secrets.MY_USERNAME }}
          git remote set-url origin https://${{ secrets.MY_ACCESS_TOKEN }}@github.com/${{ secrets.GITHUB_USERNAME}}/saarcohenn.github.io.git
          npm run deploy
